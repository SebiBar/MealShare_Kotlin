package com.example.mealshare_kotlin.ui.screens

import androidx.compose.runtime.Composable

@Composable
fun HomeScreen(
    onLogout: () -> Unit
) {

    // This function will be implemented later
    // It will contain the UI components for the Home Screen
    // For now, we can just return a placeholder or an empty composable
}